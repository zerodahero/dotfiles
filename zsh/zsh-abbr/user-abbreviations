abbr -g "git co"="git switch"
abbr -g "git cb"="git switch -c"
abbr -g "git p"="git push"
abbr -g "git pl"="git pull"
abbr -g "git df"="git diff"
abbr -g "git dfs"="git diff --staged"
abbr -g "git dfpr"="git diff --merge-base dev"
abbr -g "git st"="git status"
abbr -g "git wt"="git worktree"
abbr -g "git wta"="git worktree add -b"
abbr -g "git wtr"="git worktree remove"
abbr -g "git wtl"="git worktree list"
abbr "g"="git"
abbr "gcb"="git switch -c"
abbr "gco"="git switch"
abbr "gp"="git push"
abbr "gpl"="git pull"
abbr "gplr"="git pull --rebase"
abbr "gwa"="git-worktree-add"
abbr "kc"="kubectl"
abbr -g "kubectl g"="kubectl get"
abbr -g "kubectl ga"="kubectl get all"
abbr -g "kubectl gp"="kubectl get pods"
abbr -g "kubectl gj"="kubectl get jobs"
abbr -g "kubectl d"="kubectl describe"
abbr -g "kubectl dj"="kubectl describe job"
abbr -g "kubectl af"="kubectl apply -f"
abbr "dco"="docker compose"
